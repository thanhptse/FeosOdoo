<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="feos_contact.list">
      <field name="name">feos_contact list</field>
      <field name="model">feos_contact.feos_contact</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="feos_contact.action_window">
      <field name="name">feos_contact window</field>
      <field name="res_model">feos_contact.feos_contact</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="feos_contact.action_server">
      <field name="name">feos_contact server</field>
      <field name="model_id" ref="model_feos_contact_feos_contact"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="feos_contact" id="feos_contact.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="feos_contact.menu_1" parent="feos_contact.menu_root"/>
    <menuitem name="Menu 2" id="feos_contact.menu_2" parent="feos_contact.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="feos_contact.menu_1_list" parent="feos_contact.menu_1"
              action="feos_contact.action_window"/>
    <menuitem name="Server to list" id="feos_contact" parent="feos_contact.menu_2"
              action="feos_contact.action_server"/>
    -->
  </data>
</odoo>